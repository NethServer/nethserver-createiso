#
# Extra configuration parameters
# (each brand should define its own)
#

# Mock configuration
mock_config=nethserver-iso-7-x86_64

# Strings for ISO file name and isolinux.cfg
product_name="NethServer"
product_website="http://www.nethserver.org"
product_version=7
product_arch=x86_64

